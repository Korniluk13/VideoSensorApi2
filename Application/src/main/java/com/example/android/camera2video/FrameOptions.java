package com.example.android.camera2video;

public class FrameOptions {
    private int mWidth;
    private int mHeight;
    private int mFocalLenght;

    public FrameOptions(int width, int height, int focalLength) {
        mWidth = width;
        mHeight = height;
        mFocalLenght = focalLength;
    }
}
